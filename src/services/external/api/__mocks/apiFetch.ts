export const handleAuthResponse = jest.fn();

export const handleConfirmationCodeResponse = jest.fn();

export const callApi = jest.fn();

export const refresh = jest.fn();

export const isAccessTokenValid = jest.fn();

export const callApiProtected = jest.fn();