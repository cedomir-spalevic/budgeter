export interface Font {
   fontFamily: string;
   headerSize: number;
   regularSize: number;
   subTextSize: number;
}

export const font: Font = {
   fontFamily: "Helvetica Neue",
   headerSize: 28,
   regularSize: 18,
   subTextSize: 14
};
